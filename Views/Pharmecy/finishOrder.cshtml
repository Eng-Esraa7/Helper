@model Helper.Models.AddtoCart

@{
    ViewBag.Title = "FinishOrder";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<section class="add">
    <h3 class="center">Are you sure to Finish Order</h3>
    <div>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.name)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.price)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.price)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.noCategory)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.noCategory)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Finish" class="btn btn-default" /> |
                @Html.ActionLink("Back", "Request", "Pharmecy", new { idpharmecy = ViewBag.Id }, new { @Class = "btn" })
            </div>
        }
    </div>
</section>